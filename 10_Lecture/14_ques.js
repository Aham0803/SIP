// 14. Student Marks
// // [45, 80, 60, 90, 30]
// // Task
// // • Keep marks greater than 50.
// // • Find their average.

let sum = 0;
let count = 0;

for (let item of array) {
    if (condition) {
        sum += item;
        count++;
    }
}

let average = sum / count;
